export const environment = {
  production: true,
  // Update these values for your production environment
  apiUrl: 'https://your-api-domain.com',  // Your production API URL
  apiKey: 'your-production-api-key'        // Must match server API_KEY
};
